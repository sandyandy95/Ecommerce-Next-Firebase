const styles = (theme) => ({
  root: {
    position: 'relative',
    display: 'flex',
    flex: 1,
    flexDirection: 'column',
    height: 'auto',
    marginBottom: theme.spacing(3),
  },
  center: {
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default styles;
