const styles = (theme) => ({
  img: {
    width: '100%',
    maxWidth: 400,
    marginBottom: theme.spacing(2),
  },
  container: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    marginBottom: theme.spacing(2),
  },
});

export default styles;
